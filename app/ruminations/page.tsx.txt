import Header from '@/components/Header'
import Link from 'next/link'

// Temporary static list — we will replace this with real extracted data next
const RUMINATIONS = [
  { volume: 1, number: 1, date: 'February 1978', title: 'Vol. 1, No. 1' },
  { volume: 1, number: 2, date: 'May 1978', title: 'Vol. 1, No. 2' },
  { volume: 2, number: 1, date: '1981', title: 'Vol. 2, No. 1' },
  { volume: 2, number: 2, date: 'October 1981', title: 'The Spiritual as the Unifying Dimension' },
  // ... remaining issues will be filled from the Word documents
]

export default function RuminationsPage() {
  return (
    <main className="min-h-screen bg-[#F7F4EF]">
      <Header active="ruminations" />

      <div className="max-w-3xl mx-auto px-6 pt-12 pb-24">
        <header className="mb-12 text-center">
          <h1 className="text-3xl sm:text-4xl font-medium text-[#2C2522] mb-3 tracking-wide">
            Ruminations
          </h1>
          <p className="text-[#6B5E54] text-[17px] leading-relaxed max-w-xl mx-auto">
            Letters of analysis and comment written over more than twenty years<br />
            to friends in the Spirit.
          </p>
        </header>

        <div className="space-y-1">
          {RUMINATIONS.map((item) => (
            <div
              key={`${item.volume}-${item.number}`}
              className="group flex items-baseline gap-4 py-3 border-b border-[#E8E0D4] hover:bg-[#F0EBE3]/40 transition-colors px-2 -mx-2 rounded"
            >
              <span className="text-sm text-[#8A7B65] w-28 shrink-0 tabular-nums">
                Vol. {item.volume}, No. {item.number}
              </span>
              <span className="text-sm text-[#8A7B65] w-32 shrink-0">
                {item.date}
              </span>
              <span className="text-[#2C2522] group-hover:text-[#4A3F38] transition-colors">
                {item.title}
              </span>
            </div>
          ))}
        </div>

        <p className="mt-16 text-center text-sm text-[#8A7B65]">
          The full collection is being prepared.
        </p>
      </div>
    </main>
  )
}